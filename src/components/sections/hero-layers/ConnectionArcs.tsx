"use client";

import React from "react";

export interface ConnectionArcsProps {
  className?: string;
  disabled?: boolean;
}

/**
 * ConnectionArcs component.
 * Location markers, anchor dots, and connection arcs are now authoritatively rendered
 * by Globe.tsx using 3D spherical projection math (lat, lng + phi, theta).
 */
export const ConnectionArcs: React.FC<ConnectionArcsProps> = () => {
  return null;
};

